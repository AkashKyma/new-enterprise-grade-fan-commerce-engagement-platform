# Integration Tests (priority flows)

- Concierge session lifecycle
- NBA / offers / rewards / events endpoints
- Brazil webhooks normalization (Gupshup/Zenvia)
- Pix adapter happy path

Run locally after `docker-compose up -d db` and seeding.
