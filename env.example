NODE_ENV=development
# Backend API (avoid 3000 — Next.js and other tools often use it)
PORT=3001
DATABASE_URL=postgres://user:password@localhost:5432/fan_platform
REDIS_URL=redis://localhost:6379

# THU-17 env
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=fan_platform
JWT_SECRET=dev-secret

