# Backend HTTP port (default 3001 — avoids clashing with Next.js on 3000)
PORT=3001

# Database
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=fan_platform

# Cache / Queue
REDIS_URL=redis://localhost:6379

# Providers (placeholders)
GEMINI_API_KEY=
AWS_REGION=
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
WHATSAPP_PROVIDER=
WHATSAPP_API_KEY=

# THU-17 env
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=fan_platform
JWT_SECRET=dev-secret

# Auto-seed demo fan, shop, events, loyalty (backend startup)
SEED_DEMO=true

